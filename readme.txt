You need to have nodejs and mysql server to run this project.
Install all he dependencies by typing the following command in u terminal-> "npm install"

Then create the tables needed by mysql by running the following command in ur terminal-> "mysql -u <YOUR_USERNAME> -p < LeaveManagement.sql"
Note: You need to have mysql path in your environment to run the above command

Run ur server by typing the following command in ur terminal-> "node app"

Open the browser and go to-> "http://localhost:3000/" to access this website
